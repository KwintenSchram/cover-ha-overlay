<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_sparkles"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_sparkles"
        android:supportsRtl="true"
        android:theme="@style/Theme.CoverScreenHAOverlay"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- Main Configuration Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.CoverScreenHAOverlay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground Service controlling Overlay Window -->
        <service
            android:name=".service.CoverOverlayService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse|dataSync">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Cover screen floating smart home quick-control overlay" />
        </service>

        <!-- Boot Receiver -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!--
            Programmatic configuration channel for tooling (adb / CI / provisioning).

            exported="true" is required for adb shell to reach it at all, but reachability is not
            the authorisation: ConfigProvider rejects every call whose Binder.getCallingUid() is not
            the shell or root UID, so installed apps are refused regardless of permissions. This
            replaces the old intent-extras + /data/local/tmp path, which had no authentication.
        -->
        <provider
            android:name=".data.provider.ConfigProvider"
            android:authorities="com.haoverlay.coverscreen.config"
            android:exported="true"
            android:grantUriPermissions="false" />

        <!-- Watchdog Receiver -->
        <receiver
            android:name=".receiver.WatchdogReceiver"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>
